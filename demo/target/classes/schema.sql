create table samokatu(
    Id int not null,
    number varchar(30),
    mark varchar(30),
    dateOfBuild varchar(30),
    power varchar(30),
    phoneNumber varchar(30),
    arendDate varchar(30),
    useTime varchar(30)
    );